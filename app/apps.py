from django.apps import AppConfig


class AppConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'app'



#Super now give me the final version of views.py with full code which works fine, this is my leave channel id C0775QKR1PX and this is my manager dm id D076JL760RK. Use this and give me final version of full views.py